services:
  face-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: iot-face-service
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: unless-stopped